from collections import OrderedDict

n = int(input())
words = OrderedDict()

# Count occurrences while preserving order
for _ in range(n):
    w = input().strip()
    words[w] = words.get(w, 0) + 1

# Output number of distinct words
print(len(words))

# Output occurrences in order
print(*words.values())
